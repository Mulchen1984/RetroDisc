# RetroDisc — Dependencies

# === Core ===
pymediainfo>=6.1.0        # MediaInfo Wrapper
structlog>=24.1.0         # Structured Logging
pydantic>=2.5.0           # Data Validation & Settings
rich>=13.7.0              # Terminal UI (für CLI-Modus)
click>=8.1.0              # CLI Framework

# === Downloads ===
yt-dlp>=2024.1.0          # YouTube & Mediathek Downloads
httpx>=0.27.0             # Async HTTP Client (Mediathek-API)

# === AI / ML ===
faster-whisper>=1.2.0     # Offline-Untertitel (Base-Modell im Build)
requests>=2.31.0          # Von faster-whisper 1.2 zur Laufzeit importiert, aber upstream nicht deklariert
numpy>=1.26.0

# === Smart Edit / Highlights (optional; FFmpeg-Fallback ist eingebaut) ===
# scenedetect[opencv]>=0.6
# librosa>=0.10.0
# mediapipe>=0.10.0
# opencv-python>=4.9.0

# === Disc / ISO ===
pycdlib>=1.14.0           # Plattformunabhängige ISO-Erstellung

# === Audio ===
sounddevice>=0.4.6        # Fertig-Sound abspielen
soundfile>=0.12.1         # WAV-Dateien laden

# === Testing ===
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-mock>=3.12.0

# === Optional (Phase 4+) ===
# realesrgan-ncnn-vulkan   # Video-Upscaling (separat installieren)
# ollama                    # Lokales LLM (separat installieren)
