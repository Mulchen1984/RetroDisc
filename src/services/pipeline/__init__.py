"""RetroDisc pipeline: source detection, analysis, recommendation, persistent
job queue with resources/dependencies/recovery, workflows, events, provenance.

Integration layer that wires existing services (transcode, metadata, library,
fingerprint, temp) into real end-to-end workflows. No UI.
"""
